name: bad-tool
roles:
  lead:
    prompt: decide
    output_schema:
      properties:
        action: { enum: [INVESTIGATE, CONCLUDE] }
  worker:
    prompt: query
    output_schema: { type: object }
    tools: [splunk_search]
