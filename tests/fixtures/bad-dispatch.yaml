name: bad-dispatch
dispatch:
  mode: swarm
roles:
  lead:
    prompt: decide
    output_schema:
      properties:
        action: { enum: [INVESTIGATE, CONCLUDE] }
  workers:
    threat_hunter:
      description: queries things
      prompt: query
      output_schema: { type: object }
